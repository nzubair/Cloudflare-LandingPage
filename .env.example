# Cloudflare API Token (Workers edit permission required)
# Used by GitHub Actions for deployment (set as repository secret)
CLOUDFLARE_API_TOKEN=

# Note: KV namespace IDs are configured directly in wrangler.toml
# after creating namespaces with `wrangler kv:namespace create`
